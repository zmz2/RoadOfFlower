# Contributing

RoadOfFlower 欢迎补充真实 Skill、修正失效链接、增加官方来源、改进中文说明和提交可复现的兼容性验证。

## 两种 Skill 数据层

1. `skills/catalog.*`：固定版本的聚合索引，用于大范围发现；
2. `skills/direct/`：RoadOfFlower 直接搜索原始仓库、读取固定提交中的实际 `SKILL.md` 后形成的独立目录。

新增重要来源时，优先加入独立检索配置并保留仓库、文件路径、commit SHA、许可证线索和筛选理由。不能只复制另一份聚合清单后改个标题。

## 提交一条 Skill

请提供：名称、直接链接、发布者、分类、简短简介、原始仓库、许可证线索、已验证的平台与验证日期。不得提交仅由名称和想象描述构成的“伪 Skill”。

## 质量要求

- 链接必须指向现有页面或仓库；直接索引链接应固定到具体 commit。
- 简介不得宣称未经证实的安全性或兼容性。
- 不提交密钥、账号、付费内容副本或第三方源代码。
- 涉及命令执行、网络、账户、生产环境或敏感数据时，明确风险。
- 大型来源必须设置单仓库上限，避免数量多的仓库垄断目录。
- 独立精选要说明评分信号和排除规则；stars 不能替代质量判断。
- 数据变更后运行 `python scripts/validate_catalog.py`、`python scripts/validate_direct_catalog.py` 和 `python -m unittest discover -v`。

## 愿望

愿望可以作为社区文化保留，但不是仓库主线。请使用 `wish/<GitHub账号>/`；不要把个人愿望混入 Skill 目录或数量统计。个人内容应由本人提交或明确授权，并避免隐私、骚扰、违法内容和未授权材料。

## Pull Request

PR 描述应说明来源、变更数量、去重方式、验证命令、筛选方法和第三方许可证影响。维护者可要求拆分大批量、来源不明、单仓库占比过高或无法复现的改动。
